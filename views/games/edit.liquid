{% layout 'layout.liquid' %}
{% block content %}
<div class="container-sm">
    <form action="/games?_method=PUT" method="POST">
        <fieldset>
            <legend>Create New Game</legend>
            <label class="form-label">Title: </label>
            <input 
                type="text" 
                name="title" 
                placeholder="{{ game.title }}"
                value="{{ game.title }}"
                class="form-control"
            >
            <label class="form-label">Body:</label>
            <input 
                type="text" 
                name="body" 
                placeholder="{{ game.body }}" 
                value="{{ game.body }}" 
                class="form-control"
            >
            <label class="form-label">Amount:</label>
            <input 
                type="number" 
                name="amount"
                value="{{ game.amount }}"
                class="form-control"
            >
            <label class="form-label">Ready:</label>
            <input 
                type="checkbox" 
                name="ready" 
                class="form-check-input"
                {% if game.ready %}
                    checked
                {% endif %}
            >
        </fieldset>
        <input type="submit" value="Edit Game" class="btn btn-success">
    </form>
</div>
{% endblock %}